<h2 id="page_title">Edit '<%= @category.name %>'</h2>

<div class="skinny_wrapper wrapper_padding">
	<%= form_for (@category) do |f| %>

		<%= f.label :category %><br>
		<%= f.text_field :name %>

		<br>
		<br>

		<%= f.submit %>

	<% end %>
</div>
